For full project conventions, coding standards, and build/test commands, see `AGENTS.md` at the repository root.

For sub-project specific guidance, check for an `AGENTS.md` in the relevant project directory under `projects/`.
