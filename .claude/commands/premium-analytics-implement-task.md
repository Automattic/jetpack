---
description: Implement a premium-analytics task end-to-end — branch from origin/trunk (or continue on current branch), implement per spec, build, run UI verification + any DoD regression-injection items, changelog, commit, push, open or update PR. The default verify backend (/premium-analytics-verify-ui) runs in jetpack-ai-sandbox via Docker socket; override with VERIFY_SKILL for non-sandbox backends (no Docker required for those).
---

@../../.agents/skills/premium-analytics-implement-task.md
