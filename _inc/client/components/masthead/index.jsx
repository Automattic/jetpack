var React = require( 'react' );

const Masthead = React.createClass( {
	render: function() {
		return (
			<div className='jp-masthead'>
					<p>testing masthead</p>
			</div>
		)
	}
} );

module.exports = Masthead;