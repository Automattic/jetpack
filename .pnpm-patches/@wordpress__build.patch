diff --git a/lib/build.mjs b/lib/build.mjs
index dae3268..0747deb 100755
--- a/lib/build.mjs
+++ b/lib/build.mjs
@@ -977,6 +977,16 @@ async function inferStyleDependencies( scriptDependencies, packageName ) {
 	return styleDeps;
 }
 
+/**
+ * Format registry entries for a `return array({{ENTRIES}});` template.
+ *
+ * @param {string} entries PHP array entries, one per line, each ending in a comma.
+ * @return {string} Entries on their own lines, or '' so an empty registry is `array()`.
+ */
+function toPhpRegistryBody( entries ) {
+	return entries ? `\n${ entries }\n` : '';
+}
+
 /**
  * Generate PHP files for script module registration.
  *
@@ -1004,7 +1014,7 @@ async function generateModuleRegistrationPhp( modules, replacements ) {
 		generatePhpFromTemplate(
 			'module-registry.php.template',
 			path.join( BUILD_DIR, 'modules', 'registry.php' ),
-			{ ...replacements, '{{MODULES}}': modulesArray }
+			{ ...replacements, '{{MODULES}}': toPhpRegistryBody( modulesArray ) }
 		),
 		generatePhpFromTemplate(
 			'module-registration.php.template',
@@ -1040,7 +1050,7 @@ async function generateScriptRegistrationPhp( scripts, replacements ) {
 		generatePhpFromTemplate(
 			'script-registry.php.template',
 			path.join( BUILD_DIR, 'scripts', 'registry.php' ),
-			{ ...replacements, '{{SCRIPTS}}': scriptsArray }
+			{ ...replacements, '{{SCRIPTS}}': toPhpRegistryBody( scriptsArray ) }
 		),
 		generatePhpFromTemplate(
 			'script-registration.php.template',
@@ -1091,7 +1101,7 @@ async function generateStyleRegistrationPhp( styles, replacements ) {
 		generatePhpFromTemplate(
 			'style-registry.php.template',
 			path.join( BUILD_DIR, 'styles', 'registry.php' ),
-			{ ...replacements, '{{STYLES}}': stylesArray }
+			{ ...replacements, '{{STYLES}}': toPhpRegistryBody( stylesArray ) }
 		),
 		generatePhpFromTemplate(
 			'style-registration.php.template',
@@ -1138,9 +1148,9 @@ async function generateRoutesRegistry( routes, replacements ) {
 		'page'        => '${ route.page }',
 		'has_route'   => ${ hasRouteStr },
 		'has_content' => ${ hasContentStr },
-	)`;
+	),`;
 		} )
-		.join( ',\n' );
+		.join( '\n' );
 
 	// Generate single global registry at build/routes/registry.php
 	await generatePhpFromTemplate(
@@ -1181,7 +1191,8 @@ async function generateRoutesPhp( routes, replacements ) {
 	} );
 
 	const pageFunctions = await Promise.all( pageFunctionsPromises );
-	const pageRouteFunctionsCode = pageFunctions.join( '\n' );
+	// Each rendered template ends in a newline; the outer template adds the file's last one.
+	const pageRouteFunctionsCode = pageFunctions.join( '\n' ).trimEnd();
 
 	// Generate routes.php from template with injected page functions
 	await generatePhpFromTemplate(
@@ -1237,7 +1248,11 @@ async function generatePagesPhp( pageData, replacements ) {
 			'{{PAGE_SLUG_UNDERSCORE}}': pageSlugUnderscore,
 			'{{PREFIX}}': prefixUnderscore,
 			'{{INIT_MODULES_PHP_ARRAY}}': initModulesPhp,
-			'{{INIT_MODULES_JSON}}': JSON.stringify( page.initModules ),
+			'{{INIT_MODULES_JSON}}': page.initModules.length
+				? `array( ${ page.initModules
+						.map( toPhpStringLiteral )
+						.join( ', ' ) } )`
+				: 'array()',
 			'{{CAPABILITY}}': page.capability || DEFAULT_PAGE_CAPABILITY,
 		};
 
@@ -2191,9 +2206,9 @@ async function generateWidgetRegistry( widgets, replacements ) {
 		'category'     => ${ categoryStr },
 		'keywords'     => ${ keywordsStr },
 		'textdomain'   => ${ textdomainStr },
-	)`;
+	),`;
 		} )
-		.join( ',\n' );
+		.join( '\n' );
 
 	// Generate single global registry at build/widgets/registry.php
 	await generatePhpFromTemplate(
diff --git a/lib/wordpress-externals-plugin.mjs b/lib/wordpress-externals-plugin.mjs
index ab099e9..e0c48dd 100644
--- a/lib/wordpress-externals-plugin.mjs
+++ b/lib/wordpress-externals-plugin.mjs
@@ -379,7 +379,7 @@ export function createWordpressExternalsPlugin(
 						parts.push( `'version' => '${ version }'` );
 						const assetContent = `<?php return array(${ parts.join(
 							', '
-						) });`;
+						) });\n`;
 
 						const outputDir =
 							build.initialOptions.outdir ||
diff --git a/templates/module-registry.php.template b/templates/module-registry.php.template
index 5611677..6d4e54c 100644
--- a/templates/module-registry.php.template
+++ b/templates/module-registry.php.template
@@ -6,6 +6,4 @@
  * @package {{PREFIX}}
  */
 
-return array(
-{{MODULES}}
-);
+return array({{MODULES}});
diff --git a/templates/page-wp-admin.php.template b/templates/page-wp-admin.php.template
index 32416c5..292e0e8 100644
--- a/templates/page-wp-admin.php.template
+++ b/templates/page-wp-admin.php.template
@@ -176,7 +176,26 @@ function {{PREFIX}}_{{PAGE_SLUG_UNDERSCORE}}_wp_admin_enqueue_scripts( $hook_suf
 		( mountId, routes, initModules ) => {
 			const run = async () => {
 				const mod = await import( "@wordpress/boot" );
-				mod.initSinglePage( { mountId, routes, initModules } );
+				/*
+				 * Run the init modules here instead of delegating to
+				 * initSinglePage(): WordPress cores that bundle an older
+				 * @wordpress/boot (initModules support postdates WP 7.0's copy,
+				 * and the import map resolves @wordpress/boot to core's bundle
+				 * when core provides one) silently ignore the option, so init
+				 * modules would never execute. Running them before
+				 * initSinglePage() gives identical behavior on every core.
+				 */
+				for ( const id of initModules ?? [] ) {
+					try {
+						const initModule = await import( id );
+						if ( typeof initModule.init === "function" ) {
+							await initModule.init();
+						}
+					} catch ( error ) {
+						console.warn( "Failed to run boot init module:", id, error );
+					}
+				}
+				mod.initSinglePage( { mountId, routes } );
 			};
 			if ( document.readyState === "loading" ) {
 				document.addEventListener( "DOMContentLoaded", run );
@@ -184,7 +203,7 @@ function {{PREFIX}}_{{PAGE_SLUG_UNDERSCORE}}_wp_admin_enqueue_scripts( $hook_suf
 				run();
 			}
 		}
-		JS;
+JS;
 		wp_add_inline_script(
 			'{{PAGE_SLUG}}-wp-admin-prerequisites',
 			sprintf(
diff --git a/templates/page.php.template b/templates/page.php.template
index 75a504b..aada238 100644
--- a/templates/page.php.template
+++ b/templates/page.php.template
@@ -343,7 +343,7 @@ function {{PREFIX}}_{{PAGE_SLUG_UNDERSCORE}}_intercept_render() {
 
 		if ( ! current_user_can( '{{CAPABILITY}}' ) ) {
 			wp_die(
-				__( 'Sorry, you are not allowed to access this page.' ),
+				esc_html__( 'Sorry, you are not allowed to access this page.' ),
 				403
 			);
 		}
diff --git a/templates/script-registration.php.template b/templates/script-registration.php.template
index 10aac53..c68a4c3 100644
--- a/templates/script-registration.php.template
+++ b/templates/script-registration.php.template
@@ -63,7 +63,7 @@ function {{PREFIX}}_register_package_scripts( $scripts ) {
 	}
 
 	$scripts_data = require $scripts_file;
-	$plugin_dir   = dirname( __FILE__ );
+	$plugin_dir   = __DIR__;
 
 	foreach ( $scripts_data as $script_data ) {
 		$asset_file   = $scripts_dir . '/' . $script_data['asset'];
diff --git a/templates/script-registry.php.template b/templates/script-registry.php.template
index 6fa1327..1ad4c09 100644
--- a/templates/script-registry.php.template
+++ b/templates/script-registry.php.template
@@ -6,6 +6,4 @@
  * @package {{PREFIX}}
  */
 
-return array(
-{{SCRIPTS}}
-);
+return array({{SCRIPTS}});
diff --git a/templates/style-registration.php.template b/templates/style-registration.php.template
index b668be8..a92a80d 100644
--- a/templates/style-registration.php.template
+++ b/templates/style-registration.php.template
@@ -53,7 +53,7 @@ function {{PREFIX}}_register_package_styles( $styles ) {
 	}
 
 	$styles_data = require $styles_file;
-	$plugin_dir  = dirname( __FILE__ );
+	$plugin_dir  = __DIR__;
 
 	foreach ( $styles_data as $style_data ) {
 		{{PREFIX}}_override_style(
diff --git a/templates/style-registry.php.template b/templates/style-registry.php.template
index 33b84a7..353f26e 100644
--- a/templates/style-registry.php.template
+++ b/templates/style-registry.php.template
@@ -6,6 +6,4 @@
  * @package {{PREFIX}}
  */
 
-return array(
-{{STYLES}}
-);
+return array({{STYLES}});
