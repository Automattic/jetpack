Significance: patch
Type: fixed

The editor canvas draws the block's Display Format. A Link block shows the link and a QR block shows the code, instead of both showing the button. The QR label reads below the code on both sides, and the preview's checkout button takes its appearance from the theme the way the published one does.
