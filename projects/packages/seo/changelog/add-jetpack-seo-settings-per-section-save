Significance: minor
Type: changed

Give the SEO Settings text sections explicit Save buttons instead of auto-saving on blur — the front-page description saves per section and the title-structure editor saves per page-type row — and tidy the title-structure layout (rows separated by a divider rather than boxed, with each row's live preview and Save sharing one line). Toggle sections still save on change.
