Significance: patch
Type: changed

Refresh the connected-accounts list and the Add-account modal with the WordPress Design System: each row is now a single click-target that expands inline, the service avatars and inputs render with the new tokens, and tooltips open without delay.
