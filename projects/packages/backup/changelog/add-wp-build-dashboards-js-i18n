Significance: patch
Type: fixed
Comment: Affects the modernized wp-build dashboard, off by default (rsm_jetpack_ui_modernization_backup).

Load JS translation catalogs and stamp the package text domain on the wp-build dashboard bundles.
