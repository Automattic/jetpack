# Files not needed to be distributed in the package.
docs/            export-ignore
/TRACKING.md     export-ignore

# Files to include in the mirror repo
/dist/**         production-include

# Files not needed in the production build.
# (see also entries in the monorepo root .gitattributes)
src/js/**         production-exclude
/dist/*.css.map   production-exclude
/dist/*.js.map    production-exclude
src/identity-crisis/_inc/** production-exclude
