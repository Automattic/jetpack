Significance: patch
Type: changed
Comment: The `is_woa_site` function is now decoupled from the `at_options` option, but this is a WP.com change only

Update logic in `is_woa_site` function for host changes
