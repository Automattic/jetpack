/**
 * Threshold width (in pixels) below which preset controls collapse to a select.
 * Based on the container width, not the viewport.
 */
export const MOBILE_CONTAINER_WIDTH_THRESHOLD = 600;

/**
 * Threshold width (in pixels) for showing two months in the custom-range calendar.
 */
export const WIDE_CALENDAR_CONTAINER_THRESHOLD = 560;

/**
 * Minimum width (in pixels) the compact controls need. Layouts that place the
 * panel beside other content use this to decide when even the compact panel
 * must drop to its own row.
 */
export const COMPACT_CONTROLS_MIN_WIDTH = 280;
