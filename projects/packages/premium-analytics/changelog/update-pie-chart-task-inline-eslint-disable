Significance: patch
Type: changed
Comment: Internal tooling only — dashboard-pie-chart task spec switches `eslint-disable-next-line` to inline `eslint-disable-line` for the charts CSS import; no user-facing package change.
