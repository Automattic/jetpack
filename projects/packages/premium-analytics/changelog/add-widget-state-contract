Significance: minor
Type: added

Widgets Toolkit: add a data-agnostic WidgetState component and ReportWidget wrapper for consistent loading / error / empty state, plus a describeError descriptor mapper; migrate the Search Terms widget onto it.
