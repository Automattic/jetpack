Significance: patch
Type: changed

Search dashboard: Polish the experience selector — promote the page heading to `<h1>` and card titles to `<h2>`; drop the redundant "title + link" structure in the Embedded and Overlay action rows so only the link remains; tint the active card so it reads as active at a glance rather than relying on the small badge; replace fake search-result text in all previews with skeleton bars so the layout shape, not made-up copy, carries the eye.
