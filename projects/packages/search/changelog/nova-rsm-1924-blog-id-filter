Significance: minor
Type: added

Search: add a static `jetpack/filter-blog` radio filter for multisite networks. Registers only when `is_multisite()` is true; the radio list resolves from the legacy `blogIdFilteringLabels` option (populated via the `jetpack_instant_search_options` filter) and does not depend on aggregation buckets.
