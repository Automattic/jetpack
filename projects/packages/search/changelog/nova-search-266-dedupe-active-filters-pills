Significance: patch
Type: fixed

Search Blocks: prevent duplicate Active Filters pills and missing checkbox labels/counts when the blocks are nested inside a same-namespace Interactivity API wrapper (e.g. the collapsible filters popover).
