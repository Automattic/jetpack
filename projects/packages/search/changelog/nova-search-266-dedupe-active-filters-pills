Significance: patch
Type: fixed

Search Blocks: prevent duplicate Active Filters pills when the block is nested inside a same-namespace Interactivity API wrapper (e.g. the collapsible filters popover).
