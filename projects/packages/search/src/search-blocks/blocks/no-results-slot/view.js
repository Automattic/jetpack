// Intentionally empty: this block is fully declarative.
// Importing the store registers the shared actions/state on pages with this block.
// Styles live on the parent `no-results` container — a variant only ever
// renders inside one, and the container's stylesheet is what an empty
// container needs for its own default copy.
import 'jetpack-search/store';
