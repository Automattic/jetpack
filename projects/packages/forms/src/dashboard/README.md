# Jetpack Forms dashboard

Shared code for the Forms admin dashboard: components, hooks, stores, and the PHP that registers the admin page (`class-dashboard.php`).

The dashboard is built from `routes/` with `wp-build`, and those routes import the modules here.
