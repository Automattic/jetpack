/**
 * Entry point for the Divi 5 VideoPress Visual Builder bundle.
 *
 * Importing the module registry triggers registration via the
 * `divi.moduleLibrary.registerModuleLibraryStore.after` hook.
 */
import './modules';
