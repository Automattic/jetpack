Significance: minor
Type: added

Initializer: Split init() into per-subsystem methods and add update_init_options() so a consumer can skip subsystems it doesn't need (licensing, Speed Score, ExPlat, JITM, or Jetpack Manage) before init(). The Add License screen and its interstitial links now appear only when licensing loads, so skipping it no longer leaves buttons that 404.
