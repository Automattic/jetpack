Significance: patch
Type: fixed

AI Launchpad: fix QA-reported broken flows — persist skipped tasks so they survive reloads and count toward completion, make the Subscribe-block task completable (published content and widgets count, CTA points at the editor), and hide the Dashboard Site Setup widget when the AI Launchpad supersedes it.
