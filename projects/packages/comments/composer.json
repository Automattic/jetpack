{
	"name": "automattic/jetpack-comments",
	"description": "Replaces the default WordPress comment form with social login, subscription options, and block editing.",
	"type": "jetpack-library",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=8.1"
	},
	"suggest": {
		"automattic/jetpack-autoloader": "Allow for better interoperability with other plugins that use this package."
	},
	"repositories": [
		{
			"type": "path",
			"url": "../*",
			"options": {
				"monorepo": true
			}
		}
	],
	"minimum-stability": "dev",
	"prefer-stable": true,
	"extra": {
		"autotagger": true,
		"branch-alias": {
			"dev-trunk": "0.1.x-dev"
		},
		"changelogger": {
			"link-template": "https://github.com/Automattic/jetpack-comments/compare/v${old}...v${new}"
		},
		"mirror-repo": "Automattic/jetpack-comments",
		"textdomain": "jetpack-comments"
	}
}
