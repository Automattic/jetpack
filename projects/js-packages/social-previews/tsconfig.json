{
	"extends": "jetpack-js-tools/tsconfig.tsc.json",
	"compilerOptions": {
		"target": "esnext",
		// We use tsup for emitting files.
		"noEmit": true
	},
	// List all sources and source-containing subdirs.
	"include": [ "./src", "./tsup.config.ts", "./test" ]
}
