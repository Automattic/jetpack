# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Important: Most Code Lives Elsewhere

This package (`@automattic/publicize-components`) contains only a few stub components and hooks. The vast majority of Publicize UI components, hooks, and the Redux store live in the **Publicize package**:

**Go to**: [projects/packages/publicize/CLAUDE.md](../../packages/publicize/CLAUDE.md)

The Publicize package's `_inc/` directory contains:

- UI components in `_inc/components/`
- Custom hooks in `_inc/hooks/`
- The `jetpack-social-plugin` Redux store in `_inc/social-store/`

## What's in This Package

The `src/` directory contains a small number of components and hooks. Check `src/components/` and `src/hooks/` for the current contents.

For any substantial Social UI work, refer to the Publicize package documentation linked above.
