import { useSyncPostDataToStore } from './use-sync-post-data-to-store';

/**
 * Renders the hooks to sync post data to the social store.
 */
export function SyncPostDataToStore() {
	useSyncPostDataToStore();
}
