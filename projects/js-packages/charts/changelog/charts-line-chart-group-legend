Significance: minor
Type: changed

Charts: the line chart now supports the interactive comparison-group legend — one item per group, one click toggles the whole group, the value axis stays fixed as series are toggled, and the axes are dropped when every series is hidden.
