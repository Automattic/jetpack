Significance: minor
Type: added

Charts: emit the --a8c-charts-* token catalog at :root so themes can be overridden with CSS custom properties on any ancestor, and resolve tokens against the chart element so a scoped theme reaches SVG-painted colors.
