Significance: minor
Type: added

Charts: allow the --a8c-charts-* token catalog to be overridden anywhere inside a GlobalChartsProvider, and resolve tokens against the chart element so both CSS and JS honor the same override.
