Significance: minor
Type: added

Heatmap: add `columnGroups` for labeled, gapped column runs (compact gaps share the container's width, down to the theme's `groupGap`) and a `buildMonthCalendarHeatmapData` / `useMonthCalendarHeatmapData` layout that draws a date-keyed map as one month calendar per month sharing a single scale. The column-label row is now omitted when no column has a label, axis labels take the design system's line-height, the grid takes an `ariaLabel`, keyboard selection scrolls into view, and calendar labels stay Gregorian under any locale.
