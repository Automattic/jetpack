Significance: minor
Type: changed

Charts: comparison series that share a group now collapse into a single legend item, and an interactive legend toggles the whole group from one click. Supported in both the bar chart and the line chart, where the value axis also stays fixed as series are toggled. When every series is hidden, the grid and axes are dropped so the empty state stands on its own.
