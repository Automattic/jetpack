// Rollup apparently chokes on `rootDir`. So put most config in a special file for it,
// and load that here.
{
	"extends": "./tsconfig.rollup.json",
	"compilerOptions": {
		"declarationDir": "./build/",
		"rootDir": "./src"
	}
}
