export type JitmSlotProps = {
	/**
	 * Layout the dashboard owns: width, column span, centering. Not spacing.
	 */
	className?: string;
	/**
	 * The slot sits outside the page's padded container, so the card takes the page gutter.
	 */
	inset?: boolean;
};
