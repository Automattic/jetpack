export type JitmSlotProps = {
	/**
	 * Extra class names for the slot, for layout the dashboard owns — width, column
	 * span, centering. Do not put spacing here; see the component's docblock.
	 */
	className?: string;
};
