/**
 * Protect Scan v2 — threats screen placeholder.
 *
 * Phase 1 placeholder: returns a single line of text. Phases 2+ replace
 * the body with the real ThreatsDataViews wiring.
 *
 * @return The placeholder threats screen.
 */
export default function ThreatsScreen() {
	return <div data-testid="protect-scan-v2-threats">{ 'Protect Scan v2 — placeholder' }</div>;
}
