/**
 * Views a host opens together, behind `showFeaturesView`. They carry the
 * internal-testing badge, and they are the views the page notice speaks for.
 * MCP and Connectors ships publicly and is not one of them.
 */
export const GATED_VIEWS = [ 'overview', 'features' ];
