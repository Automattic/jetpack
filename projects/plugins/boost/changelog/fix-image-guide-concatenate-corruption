Significance: patch
Type: fixed

Image Guide: ship the JS and CSS bundles as pre-minified `.min.js`/`.min.css` so the concatenation serving path skips re-minifying them. The legacy PHP minifier silently corrupted the Svelte/ES6 JS bundle, which could make the image indicators disappear when Concatenate JS was enabled.
