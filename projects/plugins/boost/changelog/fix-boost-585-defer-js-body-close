Significance: patch
Type: fixed

Defer JS: insert the deferred scripts only at the document's real closing body tag, so a literal closing body tag inside a script, textarea, comment, or attribute is no longer corrupted.
