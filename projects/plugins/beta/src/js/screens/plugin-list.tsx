/**
 * PluginList screen stub — replaced in Task 6.
 *
 * @package
 */

import { __ } from '@wordpress/i18n';

/**
 * Stub component for the plugin list screen.
 *
 * @return The stub element.
 */
const PluginList = () => {
	return <p>{ __( 'Loading…', 'jetpack-beta' ) }</p>;
};

export default PluginList;
