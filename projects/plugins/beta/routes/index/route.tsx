// The Beta dashboard is a single screen with no sidebar inspector and no canvas
// editor, so the route needs no extra configuration. The list/manage split is
// driven by the `?plugin=` search param, read via `useSearch` in the app.
export const route = {};
